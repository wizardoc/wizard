import React, {Component, ReactNode} from 'react';

import {OrganizationPanel} from 'src/app/components';

export class OverviewOrganization extends Component {
  render(): ReactNode {
    return <OrganizationPanel />;
  }
}
