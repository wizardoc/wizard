import React, {Component, ReactNode} from 'react';
import styled from 'styled-components';

const Wrapper = styled.div``;

export class UserSettingsPhone extends Component {
  render(): ReactNode {
    return <Wrapper />;
  }
}
